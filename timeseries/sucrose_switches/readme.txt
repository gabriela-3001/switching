
To compile the fortran script, use:

gfortran ot_CD.f

Then to run the script use:

./a.out 5101

Where 5101 is the index of the desired parameter set. In our parameter grid, parameter set 1 corresponds to s=0 and c=0; parameter set 2 corresponds to s=0 and c=0.01; etc. 